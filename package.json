{
  "name": "tamper-proof-demo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp *.html *.css *.js dist/ && echo 'Build complete - static files ready in dist/'"
  }
}