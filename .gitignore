# Secrets and keys: never commit private key material.
*.key
keys/
.env

# Node
node_modules
dist

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/

# Demo artifacts (regenerated by `receipts demo`)
receipts/
receipts_tampered/
demo_data/
examples/sample_export.xlsx

# Remotion render outputs (regenerate with `npm run render:all` in animations/)
animations/out/
animations/.remotion/

# Claude Code local state (launch.json is shared; the rest is per-machine)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# OS
.DS_Store
